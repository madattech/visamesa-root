/**
 * Border radius tokens
 * xl matches the web app's pill-shaped buttons (~28px).
 */

export type Radii = {
  sm: number;
  md: number;
  lg: number;
  xl: number;
  full: number;
};

export const radii: Radii = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 28,
  full: 9999,
};
