import {buildSelectTemaScript, empadronamientoDemoProfile} from './index';

export {buildSelectTemaScript};
export const SELECT_TEMA_SCRIPT = buildSelectTemaScript(empadronamientoDemoProfile);
export default SELECT_TEMA_SCRIPT;
