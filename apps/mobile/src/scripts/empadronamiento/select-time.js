import {buildSelectTimeScript, empadronamientoDemoProfile} from './index';

export {buildSelectTimeScript};
export const SELECT_TIME_SCRIPT = buildSelectTimeScript(empadronamientoDemoProfile);
export default SELECT_TIME_SCRIPT;
